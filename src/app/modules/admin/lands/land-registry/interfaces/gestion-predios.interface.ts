export interface GestionPredios {
    // eslint-disable-next-line @typescript-eslint/naming-convention
    ID_PRED: number;
// eslint-disable-next-line @typescript-eslint/naming-convention
    COD_PRE: string;

// eslint-disable-next-line @typescript-eslint/naming-convention
    SEC_EJEC: string;
// eslint-disable-next-line @typescript-eslint/naming-convention
    COD_CPU: string;

    // eslint-disable-next-line @typescript-eslint/naming-convention
    ANIO_CART: number;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    BLOCK: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    COD_LOTE: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    COD_MZN: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    COD_SECT: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    COD_UU: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    COD_VIA: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    COORD_X: number;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    COORD_Y: number;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    CUADRA: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    FUENTE: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    ID_ARANC: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    ID_LOTE: number;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    INTERIOR: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    KM: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    LADO: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    LOT_URB: number;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    MZN_URB: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    NOM_UU: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    NOM_VIA: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    NUM_MUN: string;

    // eslint-disable-next-line @typescript-eslint/naming-convention
    PARTIDA: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    PISO: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    RAN_NUM: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    REFERENCIA: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    TIPO_UU: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    TIP_VIA: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    UBIGEO: string;

// eslint-disable-next-line @typescript-eslint/naming-convention
    TIP_DOC: string;

// eslint-disable-next-line @typescript-eslint/naming-convention
    DOC_IDEN: string;
// eslint-disable-next-line @typescript-eslint/naming-convention
    COD_CONTR: string;

// eslint-disable-next-line @typescript-eslint/naming-convention
    NOMBRE: string;

    // eslint-disable-next-line @typescript-eslint/naming-convention
    AP_PAT: string;


    // eslint-disable-next-line @typescript-eslint/naming-convention
    AP_MAT: string;
// eslint-disable-next-line @typescript-eslint/naming-convention
    DIR_FISCAL: string;
// eslint-disable-next-line @typescript-eslint/naming-convention
    ESTADO: string;
}
